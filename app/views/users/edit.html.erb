<h1>Edit user</h1>

<%= form_for @user do |f| %>
  <%= render 'form_fields', :f => f %>
  <div class="form_field">
    <%= f.submit "Update" %>
  </div>
<% end %>

<div>
    <%= gravatar_for @user %>
    <%= link_to('change gravatar', 'http://gravatar.com/emails') %>
</div>