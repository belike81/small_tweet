<% if object.errors.any? %>
  <div class="errors_container">
    <h2><%= pluralize(object.errors.count, "error") %> found!</h2>
    <p>We found problems with following fields:</p>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
